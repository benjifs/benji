---
title: links
permalink: /links/
---

<details class="text-right" open>
	<summary>Related pages</summary>
	<ul class="no-bullet">
		<li><a href="/linkroll">{blog,pod,link}roll</a></li>
	</ul>
</details>

{% set links = collections.links | reverse | limit(10) -%}
<div class="h-feed">
	{% for entry in links %}
		{%- set linkURL = entry.data['bookmark-of'] or entry.data['like-of'] %}
		{%- set postType = 'bookmark' if entry.data['bookmark-of'] else 'like' -%}
		{%- set linkTitle = entry.data.title %}
		{%- set linkContent = entry.templateContent %}
		{% include "partials/entry/link.njk" %}
		<hr>
	{%- else %}
	<h4>There are no links</h4>
	{% endfor %}
</div>